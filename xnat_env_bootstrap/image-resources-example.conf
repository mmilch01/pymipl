# nnUNet models
UPDATE_MODELS=1                                # (0 or 1): whether to update models"
ARCHIVED_MODELS_DIR="/models-repo" # directory with archived nnunet models"

#Load user environment from XNAT
LOAD_ENV=1                          # whether to load user environment from server
SERVER="https://myxnat.org"         # XNAT sever e.g. https://myxnat.org
PROJECT="MY_PROJECT_ID"             # Project where environment is stored, e.g MYPROJECT
RESOURCE="MY_PROJECT_RESOURCE_LABEL" # Project resource where environment is stored, e.g. ENVS
ENV_FILE="myenv.tar.gz"             # File name under the project resource, e.g. myenv.tar.gz
